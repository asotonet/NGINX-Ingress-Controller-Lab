apiVersion: apps/v1
kind: Deployment
metadata:
  name: webapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: webapp
  template:
    metadata:
      labels:
        app: webapp
    spec:
      containers:
      - name: webapp
        #image: f5devcentral/f5-demo-httpd:openshift
        image: cavalen/f5-demo-httpd:openshift
        env:
        - name: F5DEMO_APP
          value: "website"
        - name: F5DEMO_COLOR
          value: "6600cc"
        #  value: "009639" #NGINX GREEN
        #  value: "e21d38" #F5 RED
        ports:
        - containerPort: 8080
        readinessProbe:
          httpGet:
            port: 8080
            path: /
            httpHeaders:
            - name: server
              value: "nginx/1.19.8"
          initialDelaySeconds: 1
          periodSeconds: 5
          timeoutSeconds: 4
          successThreshold: 2
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: webapp-svc
spec:
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: webapp
